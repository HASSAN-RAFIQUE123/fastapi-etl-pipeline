[project]
name = "fastapi-etl-pipeline"
version = "0.1.0"
description = "FastAPI ETL pipeline with layered application structure"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn[standard]>=0.30.0",
  "sqlalchemy>=2.0.30",
  "python-dotenv>=1.0.0",
  "httpx>=0.27.0",
  "pytest>=8.3.0",
]
