# API Guide

The FastAPI app exposes the main data endpoints at the root level and also includes a set of placeholder routes under the new package-oriented structure.

Useful entry points:
- /health for health checks
- /customers for customer data
- /products for product data
- /orders for order data
- /feedback for feedback data
